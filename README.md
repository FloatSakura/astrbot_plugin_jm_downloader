# astrbot_plugin_jm_downloader
An AstrBot plugin for downloading comics from JMComic (18+) via QQ chat. Supports `.jm &lt;ID> [chapter range]`, outputs as encrypted ZIP / optimized PDF with merge-forward image preview.
